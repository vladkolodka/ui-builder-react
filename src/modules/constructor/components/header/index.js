import {HEADER} from 'modules/constructor/library/componentNames';

import Edit from './EditHeader';
import View from './ViewHeader';

export const name = HEADER;
const component = {Edit, View};

export default component;
